export type SafeArea = {
  top: number,
  right: number,
  bottom: number,
  left: number,
  width: number,
  height: number,
}

export type UserInfo = {
  nickName: string,
  avatarUrl?: string
}

export type TokenInfo = {
  accessToken: string
  refreshToken: string
  expiresAt: number
}

export class CryptoInitError extends Error {
  constructor(message: string) {
    super(message)
    this.name = 'CryptoInitError'
  }
}

type State = {
  lifeCycleNum: number
  statusBarHeight: number
  safeArea: SafeArea
  isDarkMode: boolean
  userInfo: UserInfo | null
  tokenInfo: TokenInfo | null
}

const STORAGE_KEYS = {
  USER_INFO: 'HY_USER_INFO',
  TOKEN_INFO: 'HY_TOKEN_INFO',
  ENCRYPTED_TOKEN: 'HY_ENCRYPTED_TOKEN'
}

let encryptKey: string | null = null

const initEncryptKey = (): string => {
  if (encryptKey) {
    return encryptKey
  }
  
  try {
    const sysInfo = uni.getSystemInfoSync?.() || {}
    const deviceId = sysInfo.deviceId || sysInfo.platform || sysInfo.model
    
    if (!deviceId) {
      throw new CryptoInitError('无法获取设备标识，无法初始化加密')
    }
    
    let hash = 0
    for (let i = 0; i < deviceId.length; i++) {
      const char = deviceId.charCodeAt(i)
      hash = ((hash << 5) - hash) + char
      hash = hash & hash
    }
    
    const key = Math.abs(hash).toString(36).padStart(8, '0') + 'hy_secure_key_'
    encryptKey = key.slice(0, 32)
    return encryptKey
  } catch (error) {
    throw new CryptoInitError('加密初始化失败')
  }
}

const base64Chars = 'ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefghijklmnopqrstuvwxyz0123456789+/'

const base64Encode = (str: string): string => {
  let result = ''
  let i = 0
  const charCodeArray = str.split('').map(c => c.charCodeAt(0))
  
  while (i < charCodeArray.length) {
    const byte1 = charCodeArray[i++]
    const byte2 = charCodeArray[i++]
    const byte3 = charCodeArray[i++]
    
    const enc1 = byte1 >> 2
    const enc2 = ((byte1 & 3) << 4) | (byte2 >> 4)
    const enc3 = ((byte2 & 15) << 2) | (byte3 >> 6)
    const enc4 = byte3 & 63
    
    result += base64Chars[enc1] + base64Chars[enc2] + 
              (byte2 === undefined ? '=' : base64Chars[enc3]) + 
              (byte3 === undefined ? '=' : base64Chars[enc4])
  }
  
  return result
}

const base64Decode = (str: string): string => {
  const base64Str = str.replace(/[^A-Za-z0-9+/=]/g, '')
  let result = ''
  let i = 0
  
  while (i < base64Str.length) {
    const enc1 = base64Chars.indexOf(base64Str[i++])
    const enc2 = base64Chars.indexOf(base64Str[i++])
    const enc3 = base64Chars.indexOf(base64Str[i++])
    const enc4 = base64Chars.indexOf(base64Str[i++])
    
    const byte1 = (enc1 << 2) | (enc2 >> 4)
    const byte2 = ((enc2 & 15) << 4) | (enc3 >> 2)
    const byte3 = ((enc3 & 3) << 6) | enc4
    
    result += String.fromCharCode(byte1)
    if (enc3 !== 64) result += String.fromCharCode(byte2)
    if (enc4 !== 64) result += String.fromCharCode(byte3)
  }
  
  return result
}

const xorEncrypt = (text: string): string => {
  const key = initEncryptKey()
  let result = ''
  for (let i = 0; i < text.length; i++) {
    result += String.fromCharCode(text.charCodeAt(i) ^ key.charCodeAt(i % key.length))
  }
  return base64Encode(result)
}

const xorDecrypt = (encrypted: string): string => {
  const key = initEncryptKey()
  const decoded = base64Decode(encrypted)
  let result = ''
  for (let i = 0; i < decoded.length; i++) {
    result += String.fromCharCode(decoded.charCodeAt(i) ^ key.charCodeAt(i % key.length))
  }
  return result
}

export const state = reactive({
  lifeCycleNum: 0,
  statusBarHeight: 0,
  safeArea: {
    top: 0,
    right: 0,
    bottom: 0,
    left: 0,
    width: 0,
    height: 0,
  },
  isDarkMode: false,
  userInfo: null,
  tokenInfo: null
} as State)

export const setLifeCycleNum = (num: number) => {
  state.lifeCycleNum = num
}

export const setStatusBarHeight = (height: number) => {
  state.statusBarHeight = height
}

export const setSafeArea = (value: SafeArea) => {
  state.safeArea = value
}

export const setUserInfo = (userInfo: UserInfo | null) => {
  state.userInfo = userInfo
  if (userInfo) {
    uni.setStorageSync(STORAGE_KEYS.USER_INFO, JSON.stringify(userInfo))
  } else {
    uni.removeStorageSync(STORAGE_KEYS.USER_INFO)
  }
}

export const setTokenInfo = (tokenInfo: TokenInfo | null): boolean => {
  state.tokenInfo = tokenInfo
  try {
    if (tokenInfo) {
      const encrypted = xorEncrypt(JSON.stringify(tokenInfo))
      uni.setStorageSync(STORAGE_KEYS.ENCRYPTED_TOKEN, encrypted)
    } else {
      uni.removeStorageSync(STORAGE_KEYS.TOKEN_INFO)
      uni.removeStorageSync(STORAGE_KEYS.ENCRYPTED_TOKEN)
    }
    return true
  } catch (error) {
    console.error('Token存储失败:', error)
    return false
  }
}

export const isTokenExpired = (): boolean => {
  if (!state.tokenInfo) return true
  return Date.now() >= state.tokenInfo.expiresAt
}

let isRefreshing = false
let refreshQueue: Array<(success: boolean) => void> = []

export const refreshToken = async (): Promise<boolean> => {
  if (!state.tokenInfo?.refreshToken) {
    return false
  }

  if (isRefreshing) {
    return new Promise((resolve) => {
      refreshQueue.push(resolve)
    })
  }

  isRefreshing = true

  return new Promise((resolve) => {
    uni.request({
      url: '/api/auth/refresh',
      method: 'POST',
      header: {
        'Content-Type': 'application/json'
      },
      data: {
        refreshToken: state.tokenInfo?.refreshToken
      },
      success: (res) => {
        try {
          const data = res.data as { success: boolean; data?: { accessToken: string; refreshToken?: string; expiresIn?: number } }
          
          if (data.success && data.data) {
            const newTokenInfo: TokenInfo = {
              accessToken: data.data.accessToken,
              refreshToken: data.data.refreshToken || state.tokenInfo!.refreshToken,
              expiresAt: Date.now() + (data.data.expiresIn || 7200) * 1000
            }
            setTokenInfo(newTokenInfo)
            resolve(true)
            refreshQueue.forEach(callback => callback(true))
          } else {
            resolve(false)
            refreshQueue.forEach(callback => callback(false))
          }
        } catch {
          resolve(false)
          refreshQueue.forEach(callback => callback(false))
        }
      },
      fail: () => {
        resolve(false)
        refreshQueue.forEach(callback => callback(false))
      },
      complete: () => {
        isRefreshing = false
        refreshQueue = []
      }
    })
  })
}

export const restoreLoginState = (): boolean => {
  try {
    const userInfoStr = uni.getStorageSync(STORAGE_KEYS.USER_INFO)
    
    if (userInfoStr) {
      state.userInfo = JSON.parse(userInfoStr) as UserInfo
    }
    
    const encryptedToken = uni.getStorageSync(STORAGE_KEYS.ENCRYPTED_TOKEN)
    if (encryptedToken) {
      try {
        const tokenInfoStr = xorDecrypt(encryptedToken)
        state.tokenInfo = JSON.parse(tokenInfoStr) as TokenInfo
      } catch {
        state.tokenInfo = null
        uni.removeStorageSync(STORAGE_KEYS.ENCRYPTED_TOKEN)
      }
    }
    
    return !!state.userInfo && !!state.tokenInfo
  } catch {
    return false
  }
}

export const clearLoginState = () => {
  state.userInfo = null
  state.tokenInfo = null
  uni.removeStorageSync(STORAGE_KEYS.USER_INFO)
  uni.removeStorageSync(STORAGE_KEYS.TOKEN_INFO)
  uni.removeStorageSync(STORAGE_KEYS.ENCRYPTED_TOKEN)
}

export const isLoggedIn = (): boolean => {
  return !!state.userInfo && !!state.tokenInfo && !isTokenExpired()
}

const resolveTheme = (appTheme: string, osTheme?: string): boolean => {
  const resolvedTheme = appTheme == "auto" ? (osTheme ?? 'light') : (appTheme ?? 'light')
  return resolvedTheme == 'dark'
}

export const checkSystemTheme = () => {
  // #ifdef WEB || MP-WEIXIN
  uni.offHostThemeChange()
  const info = uni.getAppBaseInfo();
  state.isDarkMode = info.hostTheme === 'dark';
  uni.onHostThemeChange((result) => {
    state.isDarkMode = result.hostTheme === 'dark';
  });
  // #endif
  // #ifdef APP
  uni.offAppThemeChange()
  uni.getSystemInfo({
    success: (res: GetSystemInfoResult) => {
      state.isDarkMode = resolveTheme(res.appTheme ?? 'light', res.osTheme)
    }
  })
  uni.onAppThemeChange((res: AppThemeChangeResult) => {
    state.isDarkMode = resolveTheme(res.appTheme ?? 'light', undefined)
  })
  // #endif
}