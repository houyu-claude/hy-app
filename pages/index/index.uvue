<template>
  <view class="page-container" :class="darkModeClass">
    <view class="content">
      <text class="title">{{ title }}</text>
      <text class="desc">基于 uni-app x 框架</text>
    </view>
  </view>
</template>

<script lang="uts" setup>
  import { ref } from 'vue'
  import { darkModeClass } from '@/composables/useDark.uts'

  const title = ref('houyu-app')

  onLoad((options) => {
    // #ifdef DEBUG
    console.log('Index Page Load', options)
    // #endif
  })

  onReady(() => {
    // #ifdef DEBUG
    console.log('Index Page Ready')
    // #endif
  })

  onShow(() => {
    // #ifdef DEBUG
    console.log('Index Page Show')
    // #endif
  })

  onHide(() => {
    // #ifdef DEBUG
    console.log('Index Page Hide')
    // #endif
  })
</script>

<style lang="scss">
  .page-container {
    min-height: 100vh;
    display: flex;
    align-items: center;
    justify-content: center;
    background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
  }

  .content {
    text-align: center;
    padding: 40rpx;
  }

  .title {
    display: block;
    font-size: 56rpx;
    font-weight: bold;
    color: #fff;
    margin-bottom: 20rpx;
  }

  .desc {
    font-size: 28rpx;
    color: rgba(255, 255, 255, 0.8);
  }

  .theme-dark {
    .page-container {
      background: linear-gradient(135deg, #2c3e50 0%, #4a69bd 100%);
    }
  }
</style>